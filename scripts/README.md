# Example Scripts

These are external rich script examples for Invoke.

Copy any folder from here into `%AppData%\Invoke\scripts`:

- `cliphist`
- `winget-plus`

Each folder stays fully external at runtime through its own `script.toml` and entry scripts.
